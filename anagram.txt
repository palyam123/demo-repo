def are_anagrams(s1: str, s2: str) -> bool:
   if len(s1) != len(s2):
       return False
   return sorted(s1) == sorted(s2)
print(are_anagrams("listen", "silent")) # True
print(are_anagrams("rat", "car")) # False
anargarm means 121 when it reverse it also a 121 so its anagram 

